package com.petrpol.hearthstonecards.utils;

/** Interface for fragments to handle on back button pressed */
public interface BackButtonInterface {

    /** Returns true if fragment completely handled back button press */
    boolean onBackPressed();
}
